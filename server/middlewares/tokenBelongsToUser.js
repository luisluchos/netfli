module.exports = function(req, res, next){
    //if the resource needs auth
    //get the data of decode token in req or res
    //find a user from DB by id or email
    //if we have a user
    //then next
}
