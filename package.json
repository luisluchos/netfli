{
  "name": "netfli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/bin/www",
    "dev-server": "nodemon server/bin/www",
    "build-angular": "cd netfliApp && ng build && cd..",
    "serve-angular": "cd netfliApp && ng serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express-generator": "^4.16.1",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0"
  }
}
